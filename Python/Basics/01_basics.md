# A variable is a named container that stores data in memory.
# Python automatically decides the type 
# Core data-types:
# 1) int --> whole numbers 
# 2) float --> decimals 
# 3) str --> text 
# 4) bool --> true/false

# type(age) tells you how Python interprets the value in memory

# type(age) tells you how Python interprets the value in memory

# This matters in data analysis because operations depend on type
# (e.g., numbers can be averaged; strings cannot)


# / always returns a float

# // removes the decimal part

# % is heavily used in data validation and grouping logic

# The condition evaluates to True or False-->

# Python executes only one branch

# Indentation controls scope (critical in Python

## range(5) generates: 0, 1, 2, 3, 4

## Loop variable i updates automatically

## Indentation defines loop body